{
  "name": "artifact-graph",
  "version": "1.6.0",
  "private": true,
  "description": "The platform's own wiring, drawn \u2014 instances, ports and the splines between them, on a canvas you can focus and cut down",
  "license": "Apache-2.0",
  "type": "commonjs",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "test": "s=0; for f in test/model.test.js test/layout.test.js test/routes.test.js test/screen.test.js test/page.test.js test/operator.test.js test/surface.test.js; do bare \"$f\" || s=1; done; exit $s",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "artifact-kit": "file:../artifact-kit",
    "artifact-operator": "file:../artifact-operator",
    "artifact-planner": "file:../artifact-planner",
    "artifact-protocol": "file:../artifact-protocol",
    "bare-assert": "^1.2.0",
    "bare-runtime": "^1.30.3",
    "bare-tap": "^1.0.0",
    "typescript": "^5.9.3"
  }
}
